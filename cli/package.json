{
  "name": "echo-cli",
  "version": "0.1.0",
  "description": "echo.ai CLI - pipeline triggers, search, and status over loopback API",
  "bin": {
    "echo": "./dist/cli/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test ../tests/cli/*.test.js"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.13",
    "typescript": "^4.8.2"
  }
}
